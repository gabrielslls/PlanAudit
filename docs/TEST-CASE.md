你现在是一个需要执行复杂研究任务的 AI Coding Agent。  
请严格按照以下步骤执行，并且必须使用工具进行检索、阅读、对齐和推演。  
所有步骤都需要输出详细的中间推理过程（deliberate）。

你的目标是：  
对比并统一 2026 年主流 AI Agent 工具协议，包括：  

- OpenAI MCP  
- OpenClaw（含 openclaw-cn）  
- OpenCode 工具协议  
- Azure AI Tools  
- AWS Bedrock Tools  
- Google Vertex AI Extensions  
- LangChain Tools / Runnable  
- LlamaIndex ToolSpec / QueryEngine  

最终输出一个“统一工具协议”的可行架构方案。

-------------------------------------
# 第 1 步：多源检索（必须使用工具）
请使用搜索工具检索以下内容（每项都必须检索）：

1. OpenAI MCP 官方文档与示例  
2. OpenClaw / openclaw-cn 工具协议文档  
3. OpenCode 工具调用规范  
4. Azure AI Tools 文档  
5. AWS Bedrock Tools 文档  
6. Google Vertex AI Extensions 文档  
7. LangChain Tools / Runnable / AgentExecutor 文档  
8. LlamaIndex ToolSpec / QueryEngine 文档  
9. 2024–2026 年 Agent 相关论文（ReAct、Toolformer、Voyager、AutoGPT、Devin 等）

要求：  
- 每次检索都必须调用工具  
- 每个来源都要生成独立的“文档摘要”  
- 不要合并摘要，保持上下文不断增长

-------------------------------------
# 第 2 步：长文档阅读（必须使用工具）
对每个协议提取以下内容：

- 工具 schema（参数、返回值、错误格式）  
- 工具注册机制  
- 工具发现机制  
- 工具权限模型  
- 工具生命周期（init → call → error → teardown）  
- 工具调用链路（LLM → Router → Tool → Response）  
- 错误处理机制  
- 安全模型（权限、沙箱、隔离）

要求：  
- 每个协议单独输出  
- 不要压缩  
- 保留所有细节  
- 允许上下文膨胀到 200k–500k

-------------------------------------
# 第 3 步：跨协议语义对齐（高难度推理）
请对齐以下内容：

1. MCP vs OpenClaw 的工具 schema 差异  
2. MCP vs OpenCode 的权限模型差异  
3. OpenClaw vs 云厂商工具协议的注册机制差异  
4. LangChain / LlamaIndex 与 MCP 的抽象层差异  
5. 所有协议的错误处理机制对齐  
6. 所有协议的工具生命周期对齐  
7. 所有协议的安全模型对齐  

要求：  
- 必须逐项对齐  
- 必须输出对齐表格（文本形式）  
- 必须保留所有中间推理过程  
- 不要压缩上下文

-------------------------------------
# 第 4 步：架构推演（Deliberate 模式）
基于以上对齐结果，推演：

1. “统一工具协议”的最小可行子集（MVP）  
2. “统一权限模型”的可行方案  
3. “统一错误模型”的可行方案  
4. “统一工具生命周期”的可行方案  
5. “跨协议兼容层”的架构设计  
6. “向后兼容 MCP / OpenClaw / OpenCode”的策略  
7. “云厂商工具协议适配层”的设计  

要求：  
- 必须输出架构图（文本 ASCII）  
- 必须输出模块说明  
- 必须输出数据流  
- 必须输出调用链路  
- 必须输出潜在瓶颈  
- 必须输出安全风险分析  

-------------------------------------
# 第 5 步：Self‑Correction（循环推理）
请执行以下循环 3 次：

1. 自我审查上一轮推演  
2. 找出错误、不一致、遗漏  
3. 重新对齐协议  
4. 重新生成架构  
5. 重新生成兼容层  
6. 输出修订后的最终方案  

要求：  
- 每轮都必须保留全部上下文  
- 不要删除任何内容  
- 不要压缩  
- 允许上下文无限增长  

-------------------------------------
# 最终输出
请输出：

- 统一工具协议（Unified Tool Protocol, UTP）草案  
- 完整 schema（JSON Schema）  
- 工具生命周期  
- 权限模型  
- 错误模型  
- 调用链路  
- 兼容层设计  
- 云厂商适配层设计  
- 安全模型  
- 未来演进路线图（2026–2028）

-------------------------------------

请立即开始执行第 1 步，并严格按照步骤顺序执行。

完成一步后等指示再进行下一步。