# 测试方案：火山引擎

本方案以开源AI开发工具opencode为例子，通过**中间人代理（MITM）捕获**与**本地数据库对比**的双重校验机制，透明化远程 LLM 服务的计费行为，揭露可能存在的隐藏计费倍率问题。

- **核心逻辑**：利用 `llm-proxy` 拦截客户端发出的所有 API 请求并记录日志，同时在测试前后对本地数据库进行快照与统计。
- **校验维度**：
  1. **请求一致性**：验证本地记录的对话次数与代理拦截到的实际出站请求数是否 1:1匹配。
  2. **存活与计数验证**：确保配置文件中的所有模型均真实可用，且每一个调用都能触发计费反馈。
  3. **计费透明度**：比对测试前后的控制台余额/使用量变动，推算是否存在隐藏的"单次调用最小倍率"或非预期的额外扣费。
- **取证强度**：全流程引入 `SHA256` 哈希校验，确保从配置文件到结果数据库的每一个环节都具备不可篡改性，为公益维权提供数据底座。

## 测试工具

| 工具 | 版本 |
|------|------|
| opencode | 1.3.3 |
| oh-my-opencode | 3.14 |
| OBS Studio | 30.0.2.1 |

**辅助工具：**

- `/data/forensics/volcano/llm-proxy` — 转发opencode对火山引擎的请求，用以获取火山API调用的请求ID

## 目录与文件

| 路径 | 说明 |
|------|------|
| `$HOME/.config/opencode` | opencode配置目录 |
| $HOME/.local/share/opencode/opencode.db | opencode数据库也存有与云厂商的访问数据 |
| `/data/forensics/volcano/llm-proxy/logs` | 转发API请求日志目录 |
| `/data/forensics/volcano/videos` | 屏幕录像目录 |
| `/data/forensics/volcano/evidence` | 固定证据目录 |

## 测试准备

开启OBS录像



开启三个终端：

1. 运行opencode
2. 运行`/data/forensics/volcano/llm-proxy`
3. `tail -f /data/forensics/volcano/llm-proxy/logs/requests.log`

---

## 测试步骤

### 0. 准备

```bash
echo  "检查opencode.json 、oh-my-opencode.json 全都使用了火山引擎"
date
date >/data/forensics/volcano/evidence/sha256sum.lst
grep model $HOME/.config/opencode/oh-my-opencode.json
grep baseURL $HOME/.config/opencode/opencode.json
pkill opencode
ps -ef | grep "opencode"
sha256sum $HOME/.config/opencode/oh-my-opencode.json  |tee -a /data/forensics/volcano/evidence/sha256sum.lst
sha256sum $HOME/.config/opencode/opencode.json  |tee -a /data/forensics/volcano/evidence/sha256sum.lst
```

### 1. 删除opencode数据库

```bash
rm -f $HOME/.local/share/opencode/opencode.db
ls -l $HOME/.local/share/opencode/opencode.db
```

### 2. 确认opencode数据库已经清空

```bash
opencode stats

echo "=== database query ==="
echo -n "sessions: "
opencode db "SELECT COUNT(*) FROM session;" | tail -1
echo -n "user_prompts: "
opencode db "SELECT COUNT(*) FROM message WHERE json_extract(data, '$.role') = 'user';" | tail -1
echo -n "model_responses: "
opencode db "SELECT COUNT(*) FROM message WHERE json_extract(data, '$.role') = 'assistant';" | tail -1
```

### 3. 火山方舟控制台：配置API_KEY

- 停用以前全部API_KEY
- 启用一个新API_KEY
- 在终端中执行

```bash
export VOLCENGINE_API_KEY=
env|grep VOLCEN
```

### 4. 火山方舟控制台：确认当前周期使用量

（在控制台查看）

### 5. 在新终端监控请求记录

```bash
rm -fr /data/forensics/volcano/llm-proxy/logs/*
ls -l /data/forensics/volcano/llm-proxy/logs/
touch /data/forensics/volcano/llm-proxy/logs/requests.log
tail -f /data/forensics/volcano/llm-proxy/logs/requests.log
```

### 6. 在新终端启动代理

```bash
cd /data/forensics/volcano/llm-proxy
npm start -- --target https://ark.cn-beijing.volces.com/api/coding/v3  --log-payloads  --plan
```

### 7. 开启一个会话 在opencode 粘贴研发需求

```bash

```

### 8. 停止opencode运行


```
exit
```

### 9. 停止代理运行

```bash
回车两次
```

### 10. 统计opencode访问量

```bash
opencode stats
sha256sum  $HOME/.local/share/opencode/opencode.db |tee -a /data/forensics/volcano/evidence/sha256sum.lst

echo "=== database query ==="
echo -n "sessions: "
opencode db "SELECT COUNT(*) FROM session;" | tail -1
echo -n "user_prompts: "
opencode db "SELECT COUNT(*) FROM message WHERE json_extract(data, '$.role') = 'user';" | tail -1
echo -n "model_responses: "
opencode db "SELECT COUNT(*) FROM message WHERE json_extract(data, '$.role') = 'assistant';" | tail -1
```

### 11. 从代理日志统计API访问量

```bash
sha256sum /data/forensics/volcano/llm-proxy/logs/calls.jsonl  |tee -a /data/forensics/volcano/evidence/sha256sum.lst
sha256sum /data/forensics/volcano/llm-proxy/logs/requests.log  |tee -a /data/forensics/volcano/evidence/sha256sum.lst
wc -l /data/forensics/volcano/llm-proxy/logs/requests.log
```

### 12. 保留证据

```bash
cp -p /data/forensics/volcano/llm-proxy/logs/calls.jsonl /data/forensics/volcano/evidence
cp -p /data/forensics/volcano/llm-proxy/logs/requests.log /data/forensics/volcano/evidence
cp -p $HOME/.config/opencode/oh-my-opencode.json /data/forensics/volcano/evidence
cp -p $HOME/.config/opencode/opencode.json /data/forensics/volcano/evidence
cp -p $HOME/.local/share/opencode/opencode.db /data/forensics/volcano/evidence

ls -l /data/forensics/volcano/evidence
ls -l /data/forensics/volcano/videos
```

### 13. 查看火山方舟控制台

打开火山方舟控制台，查看codingplan使用情况
